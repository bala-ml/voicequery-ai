[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "streamlit-mic-recorder"
version = "0.0.8"
description = "Custom mic recorder"
authors = [{name = "Your Name"}]
dependencies = ["streamlit"]